{
    // include:[] 制定哪些ts文件需要被编译
    // ** 表示任意目录 *表示任意文件
    "include": [
        "src/**/*"
    ],
    // exclude:[] 不进行编译的文件或者文件夹
    "exclude": [
        "src/test/*"
    ],
    // 编译选项
    "compilerOptions": {
        // 制定要编译的js版本，不知道怎么写，写一个错的，编译器会提示你
        // 'es3', 'es5', 'es6', 'es2015', 'es2016', 'es2017', 'es2018', 'es2019', 'es2020', 'esnext'
        "target": "es5",
        // 使用的模块化规范
        // 'none', 'commonjs', 'amd', 'system', 'umd', 'es6', 'es2015', 'es2020', 'esnext'
        "module": "amd",
        // 项目中使用的库,一般不需要修改
        // "lib": ["dom"]
        // 编译后文件存放位置
        "outDir": "./build",
        // 将编译后的文件合并到一个文件中 
        // "outFile": "../build/index.js",
        // 是否编译JavaScript文件
        "allowJs": false,
        // 是否检查JavaScript文件是否符合语法
        "checkJs": false,
        // 是否移除注释
        "removeComments": false,
        // 是否生成编译后的JavaScript文件
        "noEmit": false,
        // 当有错误时不生成编译文件
        "noEmitOnError": false
    }
}